[package]
name = "qsim"
version = "0.1.0"
edition = "2021"
description = "Quantum circuit state-vector simulator for the browser (WASM)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
